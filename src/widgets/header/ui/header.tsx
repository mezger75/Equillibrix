import { Button } from '@/shared/ui/button';

export const Header = () => {
    return (
        <header className="flex w-full p-4 justify-end bg-gradient-to-tr from-pink-500 to-yellow-500">
            <Button className="">Connect</Button>
        </header>
    );
};
