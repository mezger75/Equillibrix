import { DepositWithLoopingCard } from '@/features/deposit-with-looping';

export default function Home() {
    return (
        <main className="flex justify-center items-center flex-grow">
            <DepositWithLoopingCard />
        </main>
    );
}
